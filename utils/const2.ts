import { StyleSheet } from "react-native";

export const OPENSANS_REGULAR = "OpenSans-Regular";
//từ khóa : 'js object dynamic key'

export const globalStyles = StyleSheet.create ({
    globalFont : {
        fontFamily: OPENSANS_REGULAR,
    }
})